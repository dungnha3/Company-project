/**
 * Unified gradient presets.
 * Tất cả gradients trong app phải dùng từ đây.
 */
export const GRADIENTS = {
    // Modal/card headers — dùng cho tất cả modal headers
    PRIMARY: 'bg-gradient-to-r from-indigo-500 to-indigo-600',

    // Hero sections, dashboard banners
    HERO: 'bg-gradient-to-r from-indigo-600 via-indigo-600 to-purple-600',

    // Subtle backgrounds (form headers, info boxes)
    SUBTLE: 'bg-gradient-to-r from-indigo-50 to-purple-50',

    // Avatar gradients
    AVATAR: 'bg-gradient-to-br from-indigo-500 to-indigo-600',

    // Stat card gradient
    STAT: 'bg-gradient-to-br from-indigo-500 to-indigo-600',

    // Button gradients
    BTN_PRIMARY: 'bg-gradient-to-r from-indigo-600 to-indigo-600',
};
